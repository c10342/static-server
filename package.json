{
  "name": "staticServer",
  "version": "1.0.0",
  "description": "静态资源服务",
  "main": "index.js",
  "bin": {
    "staticServer": "bin/staticServer"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.6.1",
    "yargs": "^12.0.1"
  }
}
